import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  buttonTouchabe: {
    backgroundColor: "#000",
    paddingHorizontal: 8,
    paddingVertical: 4,
    width: 350,
    height: 44,
    justifyContent: "center",
    borderRadius: 8,
  },

  title: {
    color: "#FFF",
    fontSize: 20,
    textAlign: "center",
  },

  button: {
    alignSelf: "flex-end",
  },
  
});
